# 2026-05-08

## 15:55:36 +08:00 - Analysis refactor bridge cleanup slice

- Continued the unfinished `.worktrees/analysis-refactor-spec` function-first refactor.
- Focused on the highest-priority architecture issue: `src` reverse-depending on legacy `scripts`.
- Removed `load_plot_script(...)` from `src/bacteria_analysis/analyses/rdm_alignment.py`.
- Added reusable save-or-return plotting helpers in `src/bacteria_analysis/analysis_plotting.py`.
- Added a regression test to keep `rdm_alignment` from importing `analysis_plot_scripts` or `load_plot_script`.
- Validation:
  - `pixi run pytest tests/test_rdm_alignment_analysis.py tests/test_analysis_results.py -q` passed with 11 tests.
  - `pixi run pytest tests/test_rdm_alignment_analysis.py tests/test_analysis_results.py tests/test_anchor_batch_effect.py tests/test_chemical_class_rsa_analysis.py tests/test_analysis_dataset.py tests/test_neural_features.py tests/test_chemical_features.py tests/test_rdm_utils.py tests/test_stats_utils.py -q` passed with 58 tests.
  - `pixi run pytest -q` passed with 312 tests.
  - `git diff --check` passed, with only existing line-ending warnings.
- Remaining bridge dependencies are now explicit follow-up targets:
  - `src/bacteria_analysis/analyses/anchor_batch_effect.py` still loads `base_odor_date_effect_review`.
  - `src/bacteria_analysis/analyses/chemical_class_rsa.py` still loads `taxonomy_class_stability_review`.

## 16:27:34 +08:00 - Taxonomy RSA bridge cleanup slice

- Continued the `.worktrees/analysis-refactor-spec` function-first refactor under the package-boundary principle.
- Moved reusable taxonomy-class RSA statistics into `src/bacteria_analysis/taxonomy_rsa.py`.
- Moved the taxonomy RSA figure helpers used by the function-first API into `src/bacteria_analysis/analysis_plotting.py`.
- Removed the `load_plot_script("taxonomy_class_stability_review")` bridge from `src/bacteria_analysis/analyses/chemical_class_rsa.py`.
- Added a regression test so `chemical_class_rsa` cannot silently reintroduce `analysis_plot_scripts` or `load_plot_script`.
- Validation:
  - `pixi run pytest tests/test_chemical_class_rsa_analysis.py tests/test_rdm_alignment_analysis.py -q` passed with 8 tests.
  - `pixi run pytest -q` passed with 313 tests.
- Remaining bridge dependency:
  - `src/bacteria_analysis/analyses/anchor_batch_effect.py` still loads `base_odor_date_effect_review`.

## 16:36:07 +08:00 - Anchor bridge cleanup slice

- Removed the final `src -> scripts` bridge from the function-first analyses in `.worktrees/analysis-refactor-spec`.
- Moved reusable anchor-stimulus date-effect tables/statistics into `src/bacteria_analysis/anchor_effects.py`.
- Moved anchor plotting helpers used by the function-first API into `src/bacteria_analysis/analysis_plotting.py`.
- Rewired `src/bacteria_analysis/analyses/anchor_batch_effect.py` to call package helpers directly.
- Removed the now-unused `src/bacteria_analysis/analysis_plot_scripts.py` dynamic script-loading helper.
- Added a regression test so `anchor_batch_effect` cannot silently reintroduce `analysis_plot_scripts` or `load_plot_script`.
- Validation:
  - `pixi run pytest tests/test_anchor_batch_effect.py tests/test_chemical_class_rsa_analysis.py tests/test_rdm_alignment_analysis.py tests/test_analysis_results.py -q` passed with 20 tests.
  - `pixi run pytest -q` passed with 314 tests.
  - `git diff --check` passed for the touched source/test files, with only line-ending warnings.
- Current package boundary status:
  - `src/bacteria_analysis/analyses` no longer imports `analysis_plot_scripts` or dynamically loads legacy scripts.
  - Remaining script-to-script imports are still present in legacy scripts and should be handled as the next cleanup layer.

## 16:50:38 +08:00 - Anchor neuron merge cleanup

- Simplified `src/bacteria_analysis/anchor_effects.py` so neuron merging uses a generic `NEURON_GROUPS` mapping.
- Replaced the overly specific `merge_non_ase_lr`/`MERGE_GROUPS`/`KEEP_NEURONS` split with `merge_neurons(...)`.
- Added a direct unit test confirming custom pair and singleton neuron groups use the same merge path.
- Validation:
  - `pixi run pytest tests/test_anchor_batch_effect.py -q` passed with 6 tests.
  - `pixi run pytest tests/test_anchor_batch_effect.py tests/test_chemical_class_rsa_analysis.py tests/test_rdm_alignment_analysis.py tests/test_analysis_results.py -q` passed with 21 tests.
  - `pixi run pytest -q` passed with 315 tests.

## 17:12:19 +08:00 - Biological subspace helper cleanup

- Continued `.worktrees/analysis-refactor-spec` cleanup with small commits.
- Added `src/bacteria_analysis/biological_subspace.py` for shared biological-subspace helpers used by legacy review scripts.
- Moved the shared neural RDM, taxonomy QC, stimulus mapping, chemical RDM, and display-frame preparation helpers out of `scripts/plot_biological_subspace_rdm_panel.py`.
- Added public RDM panel plotting helpers in `src/bacteria_analysis/analysis_plotting.py`.
- Rewired `plot_biological_subspace_rdm_panel.py`, `plot_biological_subspace_individual_rdms.py`, `plot_biological_triple_union_rdm.py`, and the import line in `taxonomy_class_stability_review.py` to use package helpers.
- Committed the cleanup in `.worktrees/analysis-refactor-spec` as `53f636f refactor: extract biological subspace helpers`.
- Validation:
  - `pixi run pytest tests/test_biological_subspace.py tests/test_analysis_plotting.py -q` passed with 3 tests.
  - `pixi run python -m py_compile ...` passed for the touched package module and scripts.
  - `pixi run pytest -q` passed with 318 tests.
  - `rg` found no remaining script-to-script imports or script imports from private `rsa_outputs` helpers.

## 17:33:56 +08:00 - Src package layout cleanup

- Continued `.worktrees/analysis-refactor-spec` with the target `src/bacteria_analysis` layout: `io.py`, `rdm.py`, `stats.py`, `plotting.py`, `features/`, `analyses/`, and `cli/`.
- Added `src/bacteria_analysis/plotting.py`, `src/bacteria_analysis/features/`, and `src/bacteria_analysis/cli/`.
- Rewired active analysis modules and biological-subspace scripts to import through `plotting.py` and `features/*`.
- Moved feature helper implementations into `features/chemical.py`, `features/neural.py`, `features/anchor.py`, `features/biological_subspace.py`, and `features/taxonomy.py`.
- Moved plotting implementation into `plotting.py`.
- Kept old top-level modules (`analysis_plotting.py`, `chemical_features.py`, `neural_features.py`, `anchor_effects.py`, `biological_subspace.py`, `taxonomy_rsa.py`) as compatibility wrappers.
- Committed four small slices:
  - `a81e8e5 refactor: add package layout facades`
  - `fe75282 refactor: move core feature helpers into features`
  - `a2fb637 refactor: move analysis feature helpers into features`
  - `9db6868 refactor: promote plotting module`
- Validation:
  - Targeted tests passed after each slice.
  - `pixi run pytest -q` passed with 320 tests after each implementation-move slice.

## 17:52:53 +08:00 - I/O and CLI layout cleanup

- Continued `.worktrees/analysis-refactor-spec` package-layout cleanup with small commits.
- Exposed function-first dataset/result containers and `save_analysis_result` through `bacteria_analysis.io`; old `analysis_dataset.py` and `analysis_results.py` now remain as compatibility wrappers.
- Simplified preprocessing output layout to one flat output folder:
  - `trial_metadata.parquet`
  - `trial_wide_baseline_centered.parquet`
  - `trial_tensor_baseline_centered.npz`
  - `preprocessing_report.md`
- Removed default preprocessing writes for `clean/neuron_segments_clean.parquet`, `qc/preprocessing_report.json`, and nested `trial_level/` directories.
- Added `resolve_preprocessing_path()` so downstream readers support both the new flat layout and legacy `trial_level/`, `qc/`, `clean/` outputs.
- Moved core CLI logic for preprocessing, reliability, and geometry into `src/bacteria_analysis/cli/`; `scripts/run_*.py` for those workflows are now thin wrappers.
- Committed three slices:
  - `8d88ce0 refactor: expose analysis io from io module`
  - `27d4e08 refactor: flatten preprocessing output layout`
  - `688fec1 refactor: move core cli logic into package`
- Validation:
  - `pixi run pytest -q` passed with 321 tests after the I/O boundary, flat-output, and CLI-wrapper changes.
  - Targeted path/CLI tests passed: preprocessing, reliability, geometry, RSA prototype/model-space smoke coverage.

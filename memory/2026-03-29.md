# Daily Log — 2026-03-29

## 16:24 — Wrote Stage 1 method explainer document

Added a user-facing explanation document for Stage 1 reliability that describes the method and interprets the current real-data outputs in plain language.

- **Context**: The user wanted a readable explanation of what Stage 1 is doing, especially how `LOIO`, `LODO`, permutation testing, and grouped bootstrap are defined and why their reported values should be interpreted the way they are.
- **Decision**: Wrote `docs/stage1-reliability-explained.md` with step-by-step explanations of same-vs-different distance, LOIO accuracy, LODO accuracy, permutation p-values, bootstrap confidence intervals, and the current real-data values for all four views.
- **Rationale**: The implementation/spec documents are correct but too terse for interpretation; this new document is meant to be read directly during result review.
- **Tags**: #stage1 #documentation #methods #interpretation

## 23:32 — Stored Stage 1 to Stage 2 handoff in project state

Chose not to create a separate handoff markdown document and instead wrote the transition summary into machine-readable project state.

- **Context**: The user preferred keeping `docs/stage1-reliability-explained.md` as the main human-readable explanation while using state for a handoff that can be updated over time.
- **Decision**: Created `state/project-status.json` with Stage 1 completion status, formal reporting choices, current real-data metrics, Stage 2 default inputs, and the main risks to carry forward.
- **Rationale**: This keeps handoff information centralized, structured, and easy to refresh without proliferating summary documents.
- **Tags**: #state #handoff #stage1 #stage2

## 23:57 — Framed the Stage 2 entry point from the Stage 1 handoff

Reviewed the current project state, the older whole-analysis roadmap, and the Stage 1 explanation/design artifacts to decide how Stage 2 should start.

- **Context**: The user asked how to begin Stage 2 after Stage 1 was completed.
- **Decision**: Treat `state/project-status.json` as the source of truth for current phase and handoff details; treat `docs/whole-analysis-plan.md` as outdated because it still says Stage 1 has not started. Recommend starting Stage 2 with a design/plan centered on group-level neural RDM construction and RDM stability checks across individuals and dates before any RSA work.
- **Rationale**: Stage 1 already produced pooled stimulus-distance outputs, but Stage 2 needs reproducible geometry objects and stability evidence, not just pooled matrices or exploratory figures.
- **Tags**: #stage2 #planning #handoff #rdm

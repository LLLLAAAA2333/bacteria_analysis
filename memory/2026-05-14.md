# 2026-05-14

## PCA Analysis Notebook Review

- Reviewed `notebook/PCA_analysis.ipynb` for the filtered 202604-without-20260331 PCA workflow.
- Confirmed the notebook builds trial-median stimulus prototypes, applies non-ASE L/R neuron merge, uses timepoints 10:20, and compares flattened response-window PCA and signed-peak PCA against raw metabolite concentrations from `data/metabolism_raw_data.xlsx`.
- Key review finding: flattened and signed-peak PC1 give nearly identical top metabolite correlations, and the neural PC1 is strongly dominated by AWCON loading; interpretation should treat PC1 as a dominant-neuron/amplitude axis unless standardized or sensitivity-checked.
- Reran signed-peak PCA with AWCON removed. New PC1 explains 33.19% and is dominated by AWA/ASH/AWB; it is almost identical to the original signed-peak PC2 (`r=0.9896`). New PC2 explains 22.85% and is dominated by ADF.
- Added a gitignored local rerun script at `data/local_pca_awcon_masked/run_awcon_masked_signed_peak_pca.py`. It writes AWCON-removed signed-peak PC loading plots and chemical correlation CSVs under `results/local_pca_awcon_masked/`.
- Updated the local PC weight plots to sort neurons from top to bottom by signed loading value, from most positive to most negative, rather than by absolute contribution.
- Added `data/local_pca_absolute_active_scaling/run_absolute_active_scaling_sensitivity.py` as a gitignored local sensitivity analysis. It uses absolute-active mean scaling with threshold 0.1, writes PCA/loadings/chemical-correlation outputs for all-neuron and AWCON-removed variants, and writes per-stimulus active/silent neuron tables under `results/local_pca_absolute_active_scaling/`.
- Added `data/local_pca_absolute_active_scaling/plot_threshold_trace_ci.py` to plot trial mean +/- 95% CI traces by stimulus and neuron, with threshold guides and the PCA response window. Outputs are under `results/local_pca_absolute_active_scaling/threshold_trace_ci/`; within the 10:20 response window, 518/676 stimulus-neuron pairs cross mean absolute threshold 0.1.
- Extended the threshold CI plotting script to generate a PNG-only absolute-active scaled trace version alongside raw traces. Scaled traces divide full trial traces by neuron-wise absolute-active mean scales estimated from prototype 10:20 windows; 248/676 stimulus-neuron pairs exceed 1 active-mean unit in the response window.
- Updated absolute-active scale estimation to use the 10:30 post-stimulus response window rather than 10:20. The trace-CI plot now uses 10:30 for the highlighted/scored response window, and the signed-peak PCA sensitivity keeps PCA features at 10:20 while estimating neuron-wise scale factors from 10:30.
- Revised absolute-active scaling to a signed-peak-directed suprathreshold mean: for each stimulus-neuron prototype trace, choose positive frames above +0.1 if its signed peak is positive, negative frames below -0.1 if its signed peak is negative, then pool absolute magnitudes per neuron for the scale. Also changed the PCA signed-peak feature window to 10:30 and reran outputs.
- Changed the trace-CI diagnostic to generate Plotly HTML pages modeled after `visweb.py` rather than Dash: each page has 4 stimulus columns, neuron rows, visible y-axis ticks, bottom x-axis ticks, and hover values for mean, 95% CI, and N. Raw and absolute-active scaled outputs each have 13 HTML pages under `results/local_pca_absolute_active_scaling/threshold_trace_ci/`.
- Added PC1-PC3 chemical correlation outputs for the all-neuron absolute-active scaled PCA and generated `absolute_active_scaled_all_neurons_top_chemical_pc_correlations.png`, a chemical-by-PC Pearson correlation heatmap for the top 25 chemicals ranked by max absolute r.

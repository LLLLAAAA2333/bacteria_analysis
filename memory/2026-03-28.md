# Daily Log — 2026-03-28

## 11:16 — Renamed Stage 1 code targets to responsibility-based names

Updated the Stage 1 reliability implementation plan so the document keeps `Stage 1` as the project phase name while Python modules, scripts, and tests use responsibility-based names.

- **Context**: The plan mixed project-stage language with code-level naming by proposing `stage1_*` module names.
- **Decision**: Keep `Stage 1` in roadmap/spec/plan language and results directories, but rename code targets to `reliability.py`, `reliability_stats.py`, `reliability_outputs.py`, `run_reliability.py`, and matching test files.
- **Rationale**: This matches the existing `preprocessing.py` naming style, keeps code responsibilities explicit, and avoids coupling module names to the project roadmap.
- **Tags**: #naming #stage1 #documentation

## 11:44 — Reviewed Stage 1 reliability implementation

Inspected the Stage 1 reliability worktree and found concrete issues in the grouped bootstrap and overlap accounting.

- **Context**: Review request focused on eliability.py, eliability_stats.py, eliability_outputs.py, CLI wiring, and tests in the stage1-reliability worktree
- **Decision**: Report three findings: bootstrap CI targets the wrong LOIO estimand, uild_grouped_bootstrap() collapses duplicate resamples via isin, and compare_trial_arrays() overcounts overlap neurons when valid timepoints do not actually overlap
- **Rationale**: These issues either affect the published Stage 1 outputs directly or make the provided grouped-bootstrap/overlap QC behavior technically incorrect
- **Tags**: #review #stage1 #reliability #bootstrap #qc

## 11:44 — Corrected Stage 1 review log entry

Corrected the previous session note after PowerShell escape handling mangled backticked file names.

- **Context**: Same Stage 1 reliability review in the `stage1-reliability` worktree
- **Decision**: Preserve the substantive record: the review found a LOIO bootstrap-estimand mismatch, duplicate-collapse in `build_grouped_bootstrap()`, and overlap-neuron overcounting in `compare_trial_arrays()`
- **Rationale**: Future sessions need the accurate findings summary, not the malformed earlier text
- **Tags**: #review #memory-fix #stage1

## 11:49 — Implemented and executed Stage 1 reliability analysis

Completed the Stage 1 reliability modules, CLI, outputs, and tests in the `codex/stage1-reliability` worktree, then ran the full pipeline on the real preprocessed dataset.

- **Context**: Stage 1 needed overlap-aware trial comparisons, LOIO/LODO scoring, split-half analysis, permutation nulls, grouped bootstrap, figures, and a thin CLI on top of Stage 0 outputs.
- **Decision**: Added `reliability.py`, `reliability_stats.py`, `reliability_outputs.py`, `scripts/run_reliability.py`, Stage 1 fixtures/tests, and plotting dependencies; fixed reviewer-found issues around bootstrap estimands, duplicate group resampling, and overlap-neuron accounting before finalizing the run.
- **Rationale**: This satisfies the Stage 1 implementation plan end-to-end and keeps the analysis logic reusable while leaving the CLI thin.
- **Tags**: #stage1 #reliability #implementation #testing #real-data

## 11:49 — Stage 1 real-data outcome snapshot

The real-data Stage 1 run completed under `results/stage1_reliability/` with all expected tables, QC outputs, and figures.

- **Context**: `678` trials, `37` individuals, `6` dates, `19` stimuli; full test suite passed afterward (`49 passed`).
- **Decision**: Record the strongest views as `response_window` and `on_window`; same-vs-different distance gaps were positive in all views and permutation p-values were `0.009901`.
- **Rationale**: Later sessions will need the concrete artifact location and the current scientific signal before deciding whether to mark Stage 1 as passed or only cautiously passed.
- **Tags**: #stage1 #results #real-data #statistics

## 11:53 — Locked Stage 1 formal reporting scope

Narrowed the formal Stage 1 reporting scope so the main conclusion uses a single primary view and a minimal evidence chain.

- **Context**: Follow-up design discussion after the initial real-data run raised questions about whether all four views and several extra analyses should enter the formal Stage 1 conclusion.
- **Decision**: Set `response_window` as the single formal reporting view; keep `full_trajectory`, `on_window`, and `post_window` as supplementary robustness checks; keep within-date cross-individual analyses, per-date LOIO, stimulus-by-stimulus distance matrices, and split-half as supplementary rather than formal pass/fail evidence.
- **Rationale**: This keeps Stage 1 aligned with a minimal, defensible reliability gate instead of letting exploratory or interpretive analyses dilute the main conclusion.
- **Tags**: #stage1 #reporting #design #scope

## 19:47 — Made the Stage 1 primary view configurable

Added an explicit Stage 1 primary-view selector while keeping `response_window` as the default formal reporting choice, then refreshed the real-data outputs with the new summary fields.

- **Context**: Follow-up reporting work needed the formal main view to stay fixed by default while still allowing an explicit override when a later analysis or report wants to promote a different view.
- **Decision**: Added `--primary-view` to `scripts/run_reliability.py` with choices `full_trajectory`, `on_window`, `response_window`, and `post_window`; updated Stage 1 outputs to record `primary_view`, write `primary_view_summary.parquet`, and include primary-view metrics in `run_summary.json/md`; updated the design and implementation docs to describe the default-plus-override rule.
- **Rationale**: This preserves a clean default reporting path, avoids hard-coding a single view forever, and keeps every run explicit about which view is being treated as the formal main result.
- **Tags**: #stage1 #reporting #cli #documentation #testing

## 20:14 — Redrew Stage 1 formal figures around the primary view

Updated the Stage 1 plotting layer so the formal figures now center the selected primary view instead of plotting all four views in the main panels.

- **Context**: The reporting spec already said the formal evidence chain should use one main view, but the plotting code was still emitting multi-view main panels and the same-vs-different figure lacked the permutation `p` annotation.
- **Decision**: Changed the main same-vs-different, LOIO, LODO, and overlap-QC figures to filter to the selected `primary_view`; added permutation `p` annotation to the main same-vs-different figure; kept the cross-view comparison figure and split-half summary as supplementary outputs; reran the full test suite and the real-data CLI afterward.
- **Rationale**: This finally aligns the rendered Stage 1 figures with the already approved reporting rule instead of only aligning the tables and run summary.
- **Tags**: #stage1 #plotting #reporting #testing #real-data

## 23:32 — Implemented three supplementary Stage 1 analyses

Added the previously deferred supplementary analyses for within-date cross-individual same-vs-different, per-date LOIO, and stimulus-by-stimulus distance matrices, then reran the real dataset.

- **Context**: Earlier design decisions kept these analyses out of the formal evidence chain, but the user wanted them implemented so all four views could be inspected directly.
- **Decision**: Extended `reliability.py` and `reliability_outputs.py` to compute and write all three supplementary result families across all four views; added tests for the new analysis logic and smoke-tested the new artifact set; reran the Stage 1 CLI on the real data.
- **Rationale**: This preserves the formal minimal evidence chain while giving the user the date-restricted and stimulus-structured diagnostics needed to understand date effects and stimulus relationships.
- **Tags**: #stage1 #supplementary-analysis #loio #distance-matrix #testing #real-data

# Daily Log — 2026-04-01

## 00:51 — Paused Stage 3 interpretation pending improved stimulus mapping

Agreed to stop interpreting the current Stage 3 RSA magnitudes because the `data.parquet` stimuli are not credibly matched to `matrix.xlsx` samples; identical or similar `stim_name` labels are not sufficient to trust the mapping.

- **Context**: Current Stage 3 outputs show RSA similarities below about `0.1`, but the main concern is not the small magnitude by itself. The larger issue is that the neural-side stimuli and metabolite-side sample identities are not reliably aligned in the current data handoff.
- **Decision**: Treat the Stage 3 RSA framework as implemented and validated at the pipeline level, but pause scientific interpretation until the user provides a better stimulus-to-sample correspondence dataset.
- **Rationale**: Weak or ambiguous mapping can dominate the result and make any biological interpretation of the current RSA values low-confidence.
- **Tags**: #stage3 #rsa #mapping #pause #handoff

## 11:37 — Recorded versioned dataset layout and the new 20260313 Stage 1 run

The user moved the legacy raw dataset and prior derived outputs into versioned folders, then added a new raw file `data/20260313_data.parquet` and completed preprocessing plus Stage 1 reliability for that batch.

- **Context**: The archived baseline batch now lives under `data/202601/` and `results/202601/`. The new batch preprocess outputs are under `data/20260313_preprocess`, and the Stage 1 run summary is under `results/202603/stage1_reliability`.
- **Decision**: Update curated memory and project state to stop assuming `data/data.parquet` and unversioned results roots are the current canonical paths.
- **Rationale**: Future work should reference the versioned dataset layout directly, and the new Stage 1 run should be visible in session state even before deciding whether it becomes the primary downstream dataset.
- **Tags**: #data-layout #stage1 #versioning #state

## 11:44 — Interpreted the 202603 Stage 1 boundary

Compared `results/202601/stage1_reliability` against `results/202603/stage1_reliability` and found that the new batch has stronger same-vs-different separation in `on_window` and `response_window`, but it does not support any scored leave-one-date-out result because the two dates have disjoint stimulus panels.

- **Context**: `data/20260313_preprocess/trial_level/trial_metadata.parquet` shows only two dates (`20260311`, `20260313`), each with 13 distinct stimuli and zero shared stimuli. `results/202603/stage1_reliability/tables/lodo_groups.parquet` shows both held-out dates fully excluded for `missing_training_stimulus`.
- **Decision**: Treat the `202603` Stage 1 result as evidence for within-batch / cross-individual stimulus structure only, not as evidence for cross-date generalization.
- **Rationale**: `LODO = NaN` here means the cross-date validation problem is undefined under the current design, not that generalization failed numerically.
- **Tags**: #stage1 #202603 #lodo #interpretation #comparison

## 11:45 — Merged Stage 3 framework branch back to master

Merged the reviewed Stage 3 RSA framework into `master` after re-running the full repository test suite.

- **Context**: The clean integration branch `codex/stage3-rsa-task2-clean` had already passed spec review, code-quality review, and full local pytest before merge.
- **Decision**: Fast-forwarded `master` from `d35939f` to `f5f87ba`, re-ran `pixi run pytest -q` on `master`, and removed the clean Stage 3 merge worktree/branch after success.
- **Rationale**: The Stage 3 pipeline is stable enough to live on the main line even though biological interpretation remains paused until a better stimulus-to-sample mapping dataset arrives.
- **Tags**: #git #merge #stage3 #master

## 12:24 — Ran Stage 2 and Stage 3 for the 202603 batch

Executed Stage 2 geometry on `data/20260313_preprocess` into `results/202603/stage2_geometry`, then executed a short Stage 3 RSA run against that Stage 2 output with `10` permutations to validate whether the current model-space inputs are usable for this batch.

- **Context**: Stage 2 completed successfully and shows a strong pooled cross-view similarity (`0.952454`) but zero valid date-to-date comparisons because `20260311` and `20260313` have disjoint stimulus panels. Stage 3 completed structurally, but the current `data/model_space/stimulus_sample_map.csv` overlaps this batch on only `b22_0` and `b3_2`.
- **Decision**: Treat the `202603` Stage 3 output as non-interpretable input-validation output rather than a scientific result; do not spend more runtime on larger permutation counts until a new stimulus-to-sample map is prepared for the 202603 panel.
- **Rationale**: With only two mapped stimuli, each neural-vs-model RSA comparison has `n_shared_entries = 1`, so every `rsa_results` row is `score_status = invalid` and no top model can be ranked.
- **Tags**: #stage2 #stage3 #202603 #mapping #rsa #invalid

## 12:46 — Generated a dedicated 202603 model-space input directory

Created `data/model_space_202603/` for the new stimulus panel by copying the current metabolite annotation, model registry, and model membership files, then generating a new `stimulus_sample_map.csv` from `data/20260313_preprocess/trial_level/trial_metadata.parquet`.

- **Context**: The `202603` batch has 26 one-to-one `stimulus -> stim_name` pairs, and its `stim_name` values map cleanly to `matrix.xlsx` sample ids after removing the trailing `stationary` suffix (for example `A226 stationary -> A226`).
- **Decision**: Keep the chemical model definitions unchanged for now and only version the stimulus mapping layer in a new directory.
- **Rationale**: This is the minimal safe change needed to make Stage 3 consume the `202603` neural panel without overwriting the older `data/model_space/` inputs that were built around the prior dataset.
- **Tags**: #stage3 #model-space #202603 #mapping #inputs

## 14:23 — Re-ran 202603 Stage 3 with 1000 permutations

Re-executed `scripts/run_rsa.py` against `results/202603/stage2_geometry` using `data/model_space_202603` and `1000` permutations so the `202603` Stage 3 result has finer null-resolution before the upcoming Stage 1/2/3 design review.

## 23:30 — Reviewed Task 2 prototype supplement integration commit

Reviewed commit `dd5110fc9489f5980ea16fc8ad218b7e70a3f653` in the `stage3-prototype-supplement` worktree against the Task 2 spec and found the implementation compliant.

- **Context**: Verified the actual diff in `src/bacteria_analysis/rsa.py` and `tests/test_rsa.py`, checked helper behavior in `src/bacteria_analysis/rsa_prototypes.py`, and ran the four new targeted pytest cases locally.
- **Decision**: Accept the implementation as spec compliant.
- **Rationale**: The supplementary branch adds the required `prototype_inputs` path without altering the primary outputs, restricts prototype comparisons to each date's stimulus subset, marks sparse overlaps invalid, computes FDR across the full supplementary table, excludes flagged rows from top-model ranking, and emits the required pooled support/RDM outputs.
- **Tags**: #review #stage3 #rsa #prototype #task2

## 23:14 — Reviewed Stage 3 prototype helper task for production readiness

Reviewed Task 1 in `.worktrees/stage3-prototype-supplement` against the implementation plan and production-readiness criteria after the earlier spec-compliance pass.

- **Context**: Commit range `22e863c69a26bb96390d42327c1d95b081e47b22..4e46c910e519ec5439d47b947174c89feb80807e` adds `rsa_prototypes.py` and focused unit tests for grouped prototype helpers.
- **Decision**: Flag one important follow-up before merge: `build_prototype_rdm()` currently infers feature columns by excluding only `id_columns`, so grouped prototype frames that retain metadata like `date` or `stim_name` fail when Task 2 tries to build per-date neural RDMs unless callers strip those columns manually.
- **Rationale**: The current tests cover the happy path with only `stimulus` plus `f*` columns, which hides a likely Task 2 integration failure and leaves the helper interface more fragile than intended.
- **Tags**: #code-review #stage3 #prototype #integration

## 23:18 — Cleared the prototype helper follow-up review

## 00:04 — Reviewed Task 3 prototype supplement writer changes

Reviewed commit range `95d996c950bd9eb0772765b30d5e006c89673964..48c009d15336310862a9d527b7bf45991d97eb3c` in `.worktrees/stage3-prototype-supplement` for production readiness and checked the actual writer/test behavior against the Task 3 plan.

- **Context**: Inspected `src/bacteria_analysis/rsa_outputs.py` and `tests/test_rsa.py`, ran `pixi run pytest tests/test_rsa.py -q`, and reproduced rerun behavior with and without prototype outputs.
- **Decision**: Mark the change as not merge-ready until prototype parquet cleanup is addressed for reruns.
- **Rationale**: The writer removes stale `prototype_*.png` files but leaves stale prototype tables and QC parquet files in place when prototype output is narrowed or disabled, which is a likely CLI/real-data rerun trap once `--preprocess-root` lands.
- **Tags**: #review #stage3 #prototype #writer #rerun-risk

Re-reviewed the updated Task 1 helper commit at worktree HEAD `eae82e38124f5e9a9761a82980ff893cb9cb914d` after the metadata-column fix.

- **Context**: The follow-up changed `build_prototype_rdm()` to select explicit prototype feature columns and added a regression test using grouped prototype output with metadata intact.
- **Decision**: Clear the prior integration finding and mark Task 1 merge-ready.
- **Rationale**: The updated helper now keeps metadata out of numeric distance computation, the regression test exercises the intended Task 2 calling pattern, and `pixi run pytest tests/test_rsa_prototypes.py -q` passes with `5` tests.
- **Tags**: #code-review #stage3 #prototype #fix-verified

- **Context**: The rerun kept the same ranking as the fast verification run: `global_profile` remains the top primary model for both `response_window` and `full_trajectory`, with `325` shared entries in each comparison.
- **Decision**: Use this `1000`-permutation output as the current canonical `202603` Stage 3 result.
- **Rationale**: The finer permutation grid reduces the p-value floor from about `0.0099` to about `0.0010` without changing the substantive ranking, which is better for review and reporting.
- **Tags**: #stage3 #202603 #permutations #rsa #review

## 15:27 — Wrote the Stage 3 visualization redesign spec

Documented the approved Stage 3 visualization redesign as a dedicated spec that replaces the single mixed-view RDM panel with per-view figures using `sample_id` labels and a neural-driven clustering order shared by neural and model heatmaps.

- **Context**: The user requested three specific Stage 3 visualization changes and approved the design direction during brainstorming: split by view, prefer `Axxx` labels, and cluster by the neural RDM for stronger interpretation.
- **Decision**: Wrote `docs/superpowers/specs/2026-04-01-stage3-rdm-visualization-design.md` and committed it as `6d138d2`.
- **Rationale**: The figure redesign changes output contracts and tests, so it needed an explicit spec before any implementation plan or code edits.
- **Tags**: #stage3 #visualization #spec #brainstorming

## 15:34 — Wrote the Stage 3 visualization implementation plan

Turned the approved Stage 3 visualization spec into an execution-ready implementation plan that keeps the work inside `rsa_outputs.py`, updates the existing Stage 3 tests, and ends with a real-data rerun on the `202603` outputs.

- **Context**: The user approved moving from design to planning for the Stage 3 figure refresh. The plan keeps Stage 3 statistics fixed and focuses only on per-view output files, `sample_id` label resolution, and neural-driven ordering.
- **Decision**: Wrote `docs/superpowers/plans/2026-04-01-stage3-rdm-visualization-implementation.md` and committed it as `5450a57`.
- **Rationale**: The change is small enough to stay in one module but still benefits from a TDD plan because it changes file contracts, run-summary metadata, and CLI smoke expectations.
- **Tags**: #stage3 #visualization #plan #tdd
## 23:08 — Added Stage 3 prototype helper module

Implemented and committed the new prototype helper layer for Stage 3 supplementary RSA inputs and distances.

- **Context**: Added `src/bacteria_analysis/rsa_prototypes.py` and `tests/test_rsa_prototypes.py` in the stage3 prototype supplement worktree.
- **Decision**: Use tensor-backed `TrialView` values, grouped nanmean prototypes, optional wide-table loading, and overlap-aware correlation-distance RDMs with invalid pairs as `NaN`.
- **Rationale**: This keeps Task 1 small and deterministic while matching the approved Stage 3 supplement contract.
- **Tags**: #stage3 #rsa #prototypes #tests #commit

## 23:10 — Verified Stage 3 prototype helper task against spec

Reviewed the committed Task 1 implementation in the dedicated worktree and found the helper module, tests, commit message, and current pytest result all aligned with the requested contract.

- **Context**: Inspected `src/bacteria_analysis/rsa_prototypes.py`, `tests/test_rsa_prototypes.py`, and the referenced reliability helpers, then re-ran `pixi run pytest .../tests/test_rsa_prototypes.py -q` in the worktree.
- **Decision**: Mark the implementation as spec compliant.
- **Rationale**: The code uses `TrialView.values`, deterministic row-major flattening, `np.nanmean`, required support QC fields including pooled-date tracking, optional wide-table loading, and `compute_vector_distance` with invalid RDM pairs left as `NaN`.
- **Tags**: #review #stage3 #rsa #prototypes #spec

## 23:50 — Implemented Stage 3 prototype supplementary output writer

Added Stage 3 writer coverage and output generation for prototype supplementary tables, figures, and summary metadata in the isolated stage3 prototype supplement worktree.

- **Context**: Task 3 required writer-only support for prototype supplementary RSA-by-date figures, pooled prototype RDM figures, QC routing, summary fields, and stale prototype figure cleanup.
- **Decision**: Implement the feature in src/bacteria_analysis/rsa_outputs.py with targeted tests in 	ests/test_rsa.py, preserving the existing primary Stage 3 writer contract.
- **Rationale**: This keeps the change small, localized, and aligned with the existing output-layer patterns while avoiding CLI or statistics changes.
- **Tags**: #stage3 #rsa #prototype-supplement #testing

## 23:59 — Reviewed Task 3 prototype supplementary writer commit

Reviewed commit `785c90fa87d294791d236a910537a4761690f243` in the `stage3-prototype-supplement` worktree against the approved Task 3 writer spec and found one contract mismatch.

- **Context**: Inspected the actual diff in `src/bacteria_analysis/rsa_outputs.py` and `tests/test_rsa.py`, with attention to the run-summary metadata contract for prototype supplementary outputs.
- **Decision**: Reject the implementation as not yet spec compliant.
- **Rationale**: `prototype_descriptive_outputs` is populated from the QC support tables instead of the descriptive pooled prototype RDM outputs required by the approved summary contract, and the new test locks in the same incorrect expectation.
- **Tags**: #review #stage3 #rsa #prototype #task3

## 23:57 — Corrected Stage 3 prototype descriptive output summary contract

Adjusted the Stage 3 writer summary so prototype_descriptive_outputs now reports pooled prototype RDM artifact names instead of prototype QC support tables, matching the approved spec review correction.

- **Context**: Spec review found that the summary metadata contract was misreporting descriptive outputs after Task 3 landed.
- **Decision**: Point prototype_descriptive_outputs at prototype_rdm__pooled__<view> names and update the full-view and narrowed-view writer tests accordingly.
- **Rationale**: This is the smallest fix that restores contract compliance without changing any written artifacts beyond summary metadata.
- **Tags**: #stage3 #rsa #prototype-supplement #spec-review

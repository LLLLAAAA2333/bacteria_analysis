# Daily Log — 2026-04-02

## 00:07 — Fixed Stage 3 prototype rerun parquet cleanup

Added explicit cleanup for stale prototype parquet artifacts in the Stage 3 writer and expanded rerun coverage for narrowed-view and no-supplement rewrites.

- **Context**: Code review found that rerunning Stage 3 could leave stale prototype_*.parquet files in 	ables/ and qc/ even though stale prototype PNGs were already cleaned.
- **Decision**: Remove prototype_*.parquet files from 	ables/ and qc/ before each write, and verify shared prototype parquet files are rewritten with narrowed content where applicable.
- **Rationale**: This fixes the rerun bug locally in the writer without affecting primary artifacts or CLI behavior.
- **Tags**: #stage3 #rsa #prototype-supplement #rerun-bug

## 00:14 — Added Stage 3 prototype supplement CLI input

Implemented Task 4 in the prototype supplement worktree by adding optional CLI plumbing for preprocess-root and extending the Stage 3 CLI smoke coverage to exercise real prototype supplementary outputs.

- **Context**: The supplementary prototype branch already had prototype RSA generation in the core pipeline and writer, but the CLI could not yet accept preprocess inputs to activate it end to end.
- **Decision**: Add --preprocess-root as an optional CLI argument, only load prototype inputs when the flag is provided, and expand the smoke fixture with a minimal aligned preprocess/trial_level tensor dataset.
- **Rationale**: This keeps the default CLI contract unchanged while giving the supplementary branch real end-to-end coverage for prototype artifacts.
- **Tags**: #stage3 #rsa #prototype-supplement #cli #testing

## 11:05 — Captured paired prototype RDM planning state for handoff

Updated project memory and state after finishing the paired prototype RDM design pass so implementation can continue in another window without re-deriving context.

- **Context**: The feature worktree `.worktrees/stage3-prototype-supplement` already contains validated prototype supplementary outputs. I then wrote the paired prototype RDM spec (`9adc337`) and the implementation plan (`bb13570`) covering new per-date neural-vs-model comparison figures plus pooled neural/model paired prototype plots while keeping existing prototype RSA summary figures.
- **Decision**: Mark the current phase as `202603 Stage 3 outputs validated; paired prototype RDM implementation queued in feature worktree`, update `MEMORY.md`, refresh `state/project-status.json` and `state/heartbeat-state.json`, and treat the next step as execution of the plan in the isolated worktree.
- **Rationale**: The user wants to continue implementation from another window, so the persisted state now needs to point at the correct branch, commits, plan path, and remaining work instead of the older pre-mapping Stage 3 status.
- **Tags**: #state #memory #stage3 #prototype #paired-rdm #handoff

## 11:18 — Approved Task 1 prototype comparison contract review

Reviewed commit range `bb13570..baa7433` in `.worktrees/stage3-prototype-supplement` for Task 1 spec compliance only and found the new per-date prototype comparison figure contract aligned with the stated ordering requirements.

- **Context**: The review scope was limited to `src/bacteria_analysis/rsa_outputs.py` and `tests/test_rsa.py`, with explicit checks on `prototype_figure_names`, `prototype_descriptive_outputs`, and overall `figure_names` ordering.
- **Decision**: Approve `baa7433` for Task 1 spec compliance with no violations found.
- **Rationale**: The commit preserves existing `prototype_rsa__per_date__*` names, inserts `prototype_rdm_comparison__per_date__*` between per-date prototype RSA and pooled prototype RDM names, keeps descriptive outputs limited to pooled prototype RDM, and uses acceptable placeholder rendering for this stage.
- **Tags**: #code-review #stage3 #prototype #task1 #spec-compliance

## 11:22 — Confirmed Task 1 code-quality review result

Re-reviewed commit range `bb13570..baa7433` in `.worktrees/stage3-prototype-supplement` for code quality within the scoped Stage 3 writer and test files.

- **Context**: The user asked for bugs, behavioral regressions, material maintainability issues, and Task 1 test-coverage gaps only, while explicitly allowing minimal placeholder rendering for the new per-date prototype comparison figures.
- **Decision**: Approve `baa7433` with no code-quality findings in `src/bacteria_analysis/rsa_outputs.py` or `tests/test_rsa.py`.
- **Rationale**: The writer change is limited to the new figure-name contract plus placeholder files, stale prototype figure cleanup already covers the new names, and the targeted prototype-output tests passed locally.
- **Tags**: #code-review #stage3 #prototype #task1 #code-quality
## 11:33 — Flagged Task 2 prototype empty-state figure gap

Reviewed commit range aa7433..1b757cb6faeea2540821cc15b4f2817312b0c1b5 in .worktrees/stage3-prototype-supplement for Task 2 spec compliance only and found one violation in the per-date prototype comparison writer.

- **Context**: Task 2 requires a whole prototype view with no data to still write an empty-state prototype_rdm_comparison__per_date__* figure instead of omitting the figure entirely.
- **Decision**: Mark the commit as not spec-compliant for Task 2.
- **Rationale**: The writer derives comparison-figure views only from prototype_rsa_results__per_date, so a missing view drops out of prototype_figure_names and no empty-state figure is written; the narrowed-view regression test encodes the same omission.
- **Tags**: #code-review #stage3 #prototype #task2 #spec-compliance

## 12:28 — Completed paired prototype RDM implementation and real-data rerun

Finished the paired prototype RDM plan in `.worktrees/stage3-prototype-supplement`, committed the remaining smoke-test updates, and reran the real `202603` Stage 3 pipeline with unchanged `rsa_results.parquet`.

- **Context**: The work landed across `baa7433` (Task 1 contract), `1b757cb` plus follow-up fixes `b93ce0a` and `fefc534` (Task 2 per-date paired comparisons), `dee111f` (Task 3 paired pooled prototype figures), and `919b10d` (Task 4 smoke/rerun coverage). The final real-data rerun used `results/202603/stage2_geometry`, `data/20260313_preprocess`, `data/matrix.xlsx`, and `data/model_space_202603`.
- **Decision**: Keep the internal-only per-date prototype RDM payload shape in `run_stage3_rsa()` for writer consumption, preserve all external parquet schemas, and treat the updated `202603` Stage 3 figures as validated with `rsa_results.parquet` hash unchanged at `68DA26E36C8ED81F3FF6FC53963222EF27AD879B2DAC3B9C433D8DCB8820AD73`.
- **Rationale**: The writer needed per-date neural matrices to render true paired comparisons, but the external Stage 3 artifact contract should remain stable. The rerun confirms the new visual layer did not perturb the scored Stage 3 results.
- **Tags**: #stage3 #prototype #paired-rdm #validation #real-data #state

## 14:40 — Added CLI-selectable prototype aggregation and ran median comparison

Extended the Stage 3 prototype supplementary path so prototype vectors can be aggregated with `mean` or `median`, then ran a real `202603` median pass to compare against the existing mean-based figures.

- **Context**: Prototype vectors were previously hard-coded to `np.nanmean(...)` inside `build_grouped_prototypes()`. The new implementation adds `--prototype-aggregation {mean,median}` in `.worktrees/stage3-prototype-supplement`, records the choice in `run_summary.json`, and writes the median run to `results/202603_median_protoagg/stage3_rsa`.
- **Decision**: Keep `mean` as the default aggregation, but support `median` as an explicit CLI sensitivity run. The real median pass preserved the same per-date top-model assignments (`20260311 -> bile_acid`, `20260313 -> global_profile`) while changing some prototype RSA magnitudes.
- **Rationale**: This gives a low-risk way to test robustness to outlier-sensitive trial aggregation without changing the default Stage 3 contract or overwriting the existing mean-based outputs.
- **Tags**: #stage3 #prototype #aggregation #median #cli #sensitivity

# Daily Log — 2026-03-31

## 23:00 — Stage 3 model-space review findings

Reviewed `model_space.py` for Task 3 against the stage 3 spec and plan.

- **Context**: Candidate commit `ffb687fa56a1364b1de1615f002403b7b9cec71b` on top of `f7de11cf1e6648575c6dfb66d6ea4d86d0355ce8`.
- **Decision**: Flagged two issues for follow-up review.
- **Rationale**: Empty resolved feature sets are allowed to fall through to NaN RDMs instead of failing fast, and the correlation-distance helper returns misleading distance-1 outputs for zero-norm rows.
- **Tags**: #code-review #stage3 #model-space

## 23:24 — Task 7 spec compliance review

Reviewed commit range `12567dc6a04813d776e9c80bb9e8b1cf467b6613..c2085ad386d92e0ddc52b6ea578c3152661a8337` for Task 7 compliance.

- **Context**: Stage 3 RSA seed data, whitespace-normalized metabolite headers, and underpowered-model handling.
- **Decision**: No spec violations found; the commit is Task 7 compliant.
- **Rationale**: The dry run surfaced excluded underpowered models in the run summary, `global_profile` still ran, and the committed model-space inputs matched the intended real-data templates.
- **Tags**: #code-review #stage3 #rsa #spec-compliance

## 19:46 — Rejected non-finite correlation features

Added a finite-value guard to correlation-distance validation and a regression test for `NaN`, `inf`, and `-inf` inputs.

- **Context**: Task 3 final review-fix in `.worktrees/stage3-rsa-task2-clean`.
- **Decision**: Validate correlation feature matrices with `np.isfinite` before RDM computation.
- **Rationale**: Non-finite feature values can otherwise propagate into the correlation distance path and produce invalid outputs.
- **Tags**: #bugfix #stage3 #model-space #tests

## 23:47 — Completed Stage 3 Task 7 real-data seeding and dry run

Finished the Stage 3 Task 7 implementation in `.worktrees/stage3-rsa-task2-clean` and cleared both review gates.

- **Context**: Seeded real-data `data/model_space` inputs, hardened `run_stage3_rsa()` against empty or underpowered non-global models, and ran real-data CLI verification against shared `results/stage2_geometry` and `data/matrix.xlsx`.
- **Decision**: Keep `global_profile` as the always-on primary model, seed `bile_acid` with 14 obvious members, leave the remaining curated families as draft empty templates, and mark skipped models through `excluded_from_primary_ranking` without aborting the run.
- **Rationale**: Current Stage 3 progress depends more on getting a stable pooled RSA path and reviewable input templates than on prematurely curating every biochemical family.
- **Tags**: #stage3 #rsa #model-space #real-data #review

## 23:47 — Fixed real-data matrix header normalization and summary bucketing

The real matrix exposed two production issues that synthetic tests had missed.

- **Context**: `matrix.xlsx` contains several metabolite headers with leading spaces, and skipped supplementary models were being reported under `excluded_models` in `run_summary.json`.
- **Decision**: Normalize stripped metabolite headers into the returned matrix frame in `model_space.py`, and change summary family collection so `excluded_from_primary_ranking` only removes primary-tier models from `primary_models` while supplementary rows stay in `supplementary_models` unless `model_status == "excluded"`.
- **Rationale**: The first issue blocked annotation coverage on real data; the second misrepresented supplementary model state in user-facing outputs.
- **Tags**: #stage3 #bugfix #real-data #summary #tests

## 23:31 — Follow-up review found a supplementary-model summary regression

Reviewed commit range `12567dc6a04813d776e9c80bb9e8b1cf467b6613..c2085ad386d92e0ddc52b6ea578c3152661a8337` in `.worktrees/stage3-rsa-task2-clean` and found a user-visible interaction between the new RSA skip path and run-summary family classification.

- **Context**: `run_stage3_rsa()` now catches underpowered/empty-model `ValueError`s for non-global models and sets `excluded_from_primary_ranking=True` before continuing.
- **Decision**: Report a review finding for follow-up.
- **Rationale**: Downstream summary logic treats `excluded_from_primary_ranking` as a full exclusion across all tiers, so an empty supplementary model like `broad_aromatic_union` no longer appears under `supplementary_models` and is instead reported under `excluded_models`; the new tests only cover primary/draft skip cases.
- **Tags**: #code-review #stage3 #rsa #supplementary-models #run-summary

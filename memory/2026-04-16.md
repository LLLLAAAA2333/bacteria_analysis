# Daily Log бк 2026-04-16

## 00:13 | Implemented direct-mode global-profile Stage 3 input synthesis

- kind: decision
- tags: [stage3, rsa, direct-mode, model-space, worktree, validation]
- topic: direct-rsa-default-flow
- source: user + assistant
- promotion_hint: candidate
- related_state: []
- related_memory: []

Implemented Task 2 in
`H:\Process_temporary\WJH\bacteria_analysis\.worktrees\direct-rsa-default-flow`
by adding `resolve_direct_global_profile_inputs()` plus minimal runtime-only
helpers for annotation, registry, and membership synthesis in
`src/bacteria_analysis/model_space.py`.

Added two tests in `tests/test_model_space.py` to cover the happy path and the
missing-matrix-sample-id failure. Confirmed the pre-implementation failure via
import error, then ran:
`pixi run pytest tests/test_model_space.py -k "resolve_direct_global_profile_inputs or global_profile" -q`
with `6 passed, 44 deselected`.

Committed the worktree changes as:
`6ee91fb feat: add direct global-profile Stage 3 inputs`.

## 01:03 | Reviewed Task 3 direct-RSA default-flow diff

- kind: review
- tags: [stage3, rsa, direct-mode, cli, review, worktree]
- topic: direct-rsa-default-flow
- source: user + assistant
- promotion_hint: none
- related_state: []
- related_memory: []

Reviewed the Task 3 diff in
`H:\Process_temporary\WJH\bacteria_analysis\.worktrees\direct-rsa-default-flow`
between `6ee91fb` and `26016e8` for the direct-RSA default flow.

Validated the actual changes in `scripts/run_rsa.py` and
`tests/test_rsa_cli_smoke.py` against the requested branching rules and
compatibility requirements, then ran:
`pixi run pytest tests/test_rsa_cli_smoke.py -q`
with `8 passed`.

Conclusion: implementation matches the requested direct/curated/legacy mode
precedence and test scope, with one minor code-quality concern about
`run_rsa.main()` now raising exceptions while the new `cli()` wrapper preserves
the old CLI exit-code behavior.

## 01:17 | Reviewed Task 4 legacy fallback smoke-test diff

- kind: review
- tags: [stage3, rsa, cli, smoke-test, review, worktree]
- topic: direct-rsa-default-flow
- source: user + assistant
- promotion_hint: none
- related_state: []
- related_memory: []

Reviewed the Task 4 diff in
`H:\Process_temporary\WJH\bacteria_analysis\.worktrees\direct-rsa-default-flow`
between `26016e8a933127e793cd5ddd3d692ab04d5af219` and `2be048c`.

Confirmed the patch is test-only and limited to
`tests/test_rsa_cli_smoke.py`. Ran:
`pixi run pytest tests/test_rsa_cli_smoke.py -q`,
`pixi run pytest tests/test_rsa.py -q`,
`pixi run pytest tests/test_model_space.py -q`, and
`pixi run pytest -q`
with `9 passed`, `54 passed`, `50 passed`, and `249 passed`.

Conclusion: the branch is green, but the new test is largely redundant with the
existing curated smoke test and does not directly prove that the legacy
`--geometry-root` plus `--model-input-root` fallback contract is still working.

## 09:07 | Final integrated review of direct-RSA default flow

- kind: review
- tags: [stage3, rsa, direct-mode, curated-mode, legacy-fallback, review, worktree]
- topic: direct-rsa-default-flow
- source: user + assistant
- promotion_hint: none
- related_state: []
- related_memory: []

Reviewed the final integrated diff in
`H:\Process_temporary\WJH\bacteria_analysis\.worktrees\direct-rsa-default-flow`
from base `0944af004572c0ba950096c4bd1c479de1a6de62` to head `47738c9`
against the direct-RSA default-flow plan and design.

Confirmed that:
- `model_space.py` now owns the shared Stage 3 stimulus mapping helper and the
  direct-mode `global_profile` input synthesis path.
- `run_rsa.py` now defaults to direct mode when `--model-input-root` is omitted,
  while preserving preprocess-backed curated mode and the legacy
  `--geometry-root` plus `--model-input-root` fallback.
- the earlier minor fallback concern is addressed by combining the existing
  real subprocess smoke test with the new branch-level monkeypatch test.

Ran:
`pixi run pytest tests/test_model_space.py tests/test_model_space_seed.py tests/test_rsa.py tests/test_rsa_cli_smoke.py -q`
with `145 passed`, then
`pixi run pytest -q`
with `249 passed`.

Conclusion: no material bugs, regressions, or missing must-fix tests were found
in the integrated change set. The only remaining note is low-risk: in-process
callers of `scripts/run_rsa.main()` now see exceptions while the CLI entrypoint
behavior remains stable through `cli()`.

## 09:26 | Captured the direct-RSA default workflow in memory

- kind: decision
- tags: [rsa, workflow, direct-mode, matrix-input, model-space, memory]
- topic: rsa-workflow
- source: user + assistant
- promotion_hint: promoted
- related_state: [state/heartbeat-state.json]
- related_memory: [MEM-20260416-001]

Recorded the approved user-facing pipeline as `preprocess -> reliability ->
rsa`, with `rsa` consuming `matrix.xlsx` and `preprocess_root` directly by
default. `model_space` remains available as an optional curated path for future
submodel work, but it is no longer the required default entry point for Stage 3
RSA.

## 10:53 | Re-ran 202603 preprocess, reliability, and geometry on master

- kind: execution
- tags: [202603, preprocess, reliability, geometry, heatmap, master]
- topic: 202603-rerun
- source: user + assistant
- promotion_hint: none
- related_state: [state/heartbeat-state.json]
- related_memory: [MEM-20260416-001]

Confirmed that `master` already points at the direct-RSA default-flow commit,
so no additional merge was required. Re-ran:
`pixi run python scripts/run_preprocessing.py --input data/202603_data.parquet --output-root data/202603/202603_preprocess`,
`pixi run python scripts/run_reliability.py --input-root data/202603/202603_preprocess --output-root results/202603`,
and
`pixi run python scripts/run_geometry.py --input-root data/202603/202603_preprocess --output-root results/202603`.

The rerun produced updated Stage 1 outputs under `results/202603/reliability`
and Stage 2 outputs under `results/202603/geometry`, including the pooled RDM
heatmap at `results/202603/geometry/figures/rdm_matrix__response_window__pooled.png`.

## 12:07 | Updated project-status to the refreshed 202603 baseline

- kind: state
- tags: [state, 202603, preprocess, reliability, geometry, direct-rsa]
- topic: project-status
- source: user + assistant
- promotion_hint: none
- related_state: [state/project-status.json, state/heartbeat-state.json]
- related_memory: [MEM-20260416-001]

Replaced the stale 2026-04-13 project-status summary with the current 202603
baseline: `master` already contains the direct-RSA default-flow change set,
`data/202603_data.parquet` has been reprocessed to
`data/202603/202603_preprocess`, refreshed Stage 1 outputs live under
`results/202603/reliability`, refreshed Stage 2 outputs live under
`results/202603/geometry`, and Stage 2 geometry remains separate from Stage 3
RSA by design.

## 12:14 | Organized the 202603 results directory

- kind: execution
- tags: [results, 202603, archive, compatibility, cleanup]
- topic: results-layout
- source: user + assistant
- promotion_hint: none
- related_state: [state/heartbeat-state.json]
- related_memory: []

Performed a non-destructive cleanup of `results/202603`: moved the isolated
`0313/` rerun to `results/202603/archive/20260313_rsa_rerun` and added
`results/202603/README.md` to mark `reliability/`, `geometry/`, and `rsa/` as
the current canonical outputs while keeping `stage1_reliability/`,
`stage2_geometry/`, and `stage3_rsa/` in place for historical document
compatibility.

## 16:05 | Drafted the RSA distance-method comparison spec in an isolated worktree

- kind: design
- tags: [rsa, metabolism, method-comparison, global-profile, response-window, spec, worktree]
- topic: rsa-distance-method-comparison
- source: user + assistant
- promotion_hint: candidate
- related_state: [state/heartbeat-state.json]
- related_memory: []

Created the isolated worktree
`H:\Process_temporary\WJH\bacteria_analysis\.worktrees\rsa-distance-method-comparison`
on branch `codex/rsa-distance-method-comparison`, verified a clean baseline with
`pixi run pytest tests/test_model_space.py -q` (`50 passed`), and wrote the
approved design spec to
`docs/superpowers/specs/2026-04-16-rsa-distance-method-comparison-design.md`.

The spec fixes this slice to the current `202603` dataset, uses only the pooled
`response_window` neural RDM and `global_profile`, compares four metabolite-side
distance methods, and keeps the output explicitly exploratory for manual review
without changing the current production Stage 3 contract.

## 16:35 | Wrote the implementation plan for the RSA distance-method comparison

- kind: design
- tags: [rsa, metabolism, method-comparison, plan, global-profile, response-window, worktree]
- topic: rsa-distance-method-comparison
- source: user + assistant
- promotion_hint: candidate
- related_state: [state/heartbeat-state.json]
- related_memory: []

Wrote the implementation plan to
`H:\Process_temporary\WJH\bacteria_analysis\.worktrees\rsa-distance-method-comparison\docs\superpowers\plans\2026-04-16-rsa-distance-method-comparison-implementation.md`.

The plan keeps the slice constrained to three new files:
`src/bacteria_analysis/rsa_distance_method_comparison.py`,
`scripts/run_rsa_distance_method_comparison.py`, and
`tests/test_rsa_distance_method_comparison.py`, with four execution tasks:
test scaffold, core comparison module, review-artifact writer plus runner, and
final verification on both fixture data and the real `202603` dataset.

## 16:47 | Reviewed the Task 1 test scaffold commit

- kind: review
- tags: [rsa, metabolism, method-comparison, tests, review, fixture-contract]
- topic: rsa-distance-method-comparison
- source: user + assistant
- promotion_hint: none
- related_state: [state/heartbeat-state.json]
- related_memory: []

Reviewed commit `d9a1a176caefc58c5c980343b0a716d26a6e55f7` against
`41a5b15051292d982118a7aaa30af511f2021714` in the isolated
`rsa-distance-method-comparison` worktree.

Confirmed that the diff only adds
`tests/test_rsa_distance_method_comparison.py` and that
`pixi run pytest tests/test_rsa_distance_method_comparison.py -q` fails during
collection with `ModuleNotFoundError` for
`bacteria_analysis.rsa_distance_method_comparison` as intended.

Found one contract issue in the scaffold: the robust-Euclidean fixture makes
both `flat_feature` and `feature_b` zero-IQR while the test still expects
`n_features_retained == 2`, so the failing test file does not correctly lock
the intended zero-IQR filtering behavior.

## 16:54 | Re-reviewed the latest Task 1 scaffold head commit

- kind: review
- tags: [rsa, metabolism, method-comparison, tests, review, fixture-contract]
- topic: rsa-distance-method-comparison
- source: user + assistant
- promotion_hint: none
- related_state: [state/heartbeat-state.json]
- related_memory: []

Reviewed commit `8b5c138202929010ecf54308a6bdcd8cd31e54e1` against
`41a5b15051292d982118a7aaa30af511f2021714` in the isolated
`rsa-distance-method-comparison` worktree.

Confirmed the change scope is still limited to
`tests/test_rsa_distance_method_comparison.py` and that
`pixi run pytest tests/test_rsa_distance_method_comparison.py -q` still fails
during collection with `ModuleNotFoundError` for
`bacteria_analysis.rsa_distance_method_comparison` as intended.

Found two scaffold issues: the robust-Euclidean fixture/test now locks a
different zero-IQR contract than the written plan by asserting only one
retained feature and dropping `feature_b`, and the synthetic tensor payload is
not valid preprocessing output because it uses only 3 neurons and 8 timepoints
instead of the canonical reliability tensor shape.

## 17:08 | Reviewed the current Task 1 scaffold head commit

- kind: review
- tags: [rsa, metabolism, method-comparison, tests, review, fixture-contract]
- topic: rsa-distance-method-comparison
- source: user + assistant
- promotion_hint: none
- related_state: [state/heartbeat-state.json]
- related_memory: []

Reviewed head `c00c6bbd3ae3c394c9e5939dd8dc5bc83a73d3ae` against base
`41a5b15051292d982118a7aaa30af511f2021714` in the isolated
`rsa-distance-method-comparison` worktree.

Confirmed that the earlier scaffold-contract problems are fixed: the synthetic
tensor now uses the canonical neuron/timepoint dimensions and the robust
Euclidean fixture leaves `feature_a` and `feature_b` as varying retained
features while keeping only `flat_feature` zero-IQR.

Ran:
`pixi run python -m pytest tests/test_rsa_distance_method_comparison.py -q`
and confirmed the only failure is the intended collection-time
`ModuleNotFoundError` for `bacteria_analysis.rsa_distance_method_comparison`.

Conclusion: no critical or important test-quality issues remain in the new
scaffold. Only minor maintenance nits remain around redundant local `sys.path`
bootstrapping and a zero-IQR fixture branch that duplicates the baseline
matrix.

## 17:18 | Reviewed the Task 2 core comparison module implementation

- kind: review
- tags: [rsa, metabolism, method-comparison, review, resolver, worktree]
- topic: rsa-distance-method-comparison
- source: user + assistant
- promotion_hint: none
- related_state: [state/heartbeat-state.json]
- related_memory: []

Reviewed commit `70c16d978ccac5935cd422ef430dd6f72886fd82` against
`c00c6bbd3ae3c394c9e5939dd8dc5bc83a73d3ae` in the isolated
`rsa-distance-method-comparison` worktree.

Confirmed the diff stays within the two owned files:
`src/bacteria_analysis/rsa_distance_method_comparison.py` and
`tests/test_rsa_distance_method_comparison.py`.

Ran the required focused command:
`pixi run pytest tests/test_rsa_distance_method_comparison.py -k "resolve_shared_repo_root or raw_euclidean or robust_euclidean or aitchison or current_correlation" -q`
and observed `5 passed, 1 deselected`.

Found one spec issue in the new implementation: `resolve_shared_repo_root()`
checks for `.git` before it reaches the `.worktrees` parent, so in a real git
worktree it resolves to the worktree root rather than the shared repository
root. The synthetic resolver test does not create a worktree `.git` file, so it
misses that failure mode.

## 17:39 | Re-reviewed the Task 2 core comparison head

- kind: review
- tags: [rsa, metabolism, method-comparison, review, task2, regression]
- topic: rsa-distance-method-comparison
- source: user + assistant
- promotion_hint: none
- related_state: [state/heartbeat-state.json]
- related_memory: []

Reviewed head `339e36929760485d8703bbbee27e9db8db4e5b77` against
`c00c6bbd3ae3c394c9e5939dd8dc5bc83a73d3ae` in the isolated
`rsa-distance-method-comparison` worktree.

Confirmed the diff still stays within
`src/bacteria_analysis/rsa_distance_method_comparison.py` and
`tests/test_rsa_distance_method_comparison.py`.

Ran:
`pixi run pytest tests/test_rsa_distance_method_comparison.py -q`
and observed `1 failed, 6 passed`. The failure is
`AttributeError: module 'bacteria_analysis.rsa_distance_method_comparison' has
no attribute 'run_distance_method_comparison'`.

Conclusion: the new core module regresses the preserved Task 1 smoke-test
contract by omitting `run_distance_method_comparison(...)`. Also noted a test
quality gap: the new method tests mostly check shape/metadata, not
hand-computable numeric distances or RSA values, so an incorrect Euclidean/RSA
implementation could still pass.

## 19:40 | Completed RSA distance-method comparison workflow

- kind: implementation
- tags: [rsa, metabolism, method-comparison, global-profile, response-window]
- topic: rsa-distance-method-comparison
- source: user + assistant
- promotion_hint: keep
- related_state: [state/heartbeat-state.json]
- related_memory: []

Completed the isolated `codex/rsa-distance-method-comparison` worktree workflow
for comparing four `global_profile` metabolite RDM distance methods against the
pooled neural `response_window` RDM.

Created the focused comparison module and one-off runner:
`src/bacteria_analysis/rsa_distance_method_comparison.py` and
`scripts/run_rsa_distance_method_comparison.py`. The implementation preserves
the production Stage 3 path and writes review artifacts under the shared main
workspace root:
`results/202603/rsa_distance_method_comparison`.

Verification:

- `pixi run pytest tests/test_rsa_distance_method_comparison.py -q` -> `7 passed`
- `pixi run pytest tests/test_rsa_distance_method_comparison.py tests/test_model_space.py tests/test_rsa.py -q` -> `111 passed`
- `pixi run python scripts/run_rsa_distance_method_comparison.py` -> exit `0`

Real-data summary from `method_comparison_summary.parquet`:

- `aitchison_clr_euclidean`: RSA `0.198271`, `741` shared entries
- `current_correlation`: RSA `0.197824`, `741` shared entries
- `log1p_robust_euclidean`: RSA `0.036732`, `741` shared entries
- `raw_euclidean`: RSA `-0.002011`, `741` shared entries

The generated package includes all four model RDM parquet files, the neural
`response_window` RDM parquet, `method_comparison_summary.parquet`,
`rdm_method_comparison_panel.png`, `rsa_similarity_by_method.png`, and
`run_summary.md`.

## 19:54 | Added per-date RSA distance-method comparison figures

- kind: implementation
- tags: [rsa, metabolism, method-comparison, per-date, visualization]
- topic: rsa-distance-method-comparison
- source: user + assistant
- promotion_hint: keep
- related_state: [state/heartbeat-state.json]
- related_memory: []

Extended the isolated `codex/rsa-distance-method-comparison` worktree with
per-date `response_window` comparison figures for the four `global_profile`
metabolite distance methods. The new runner is
`scripts/run_rsa_distance_method_per_date_comparison.py`.

The per-date figures order every panel by the corresponding date-specific
neural RDM clustering order, then apply that order to the four metabolite RDMs.
Generated outputs under
`results/202603/rsa_distance_method_comparison/figures`:

- `rdm_method_comparison_panel__per_date_response_window__neural_order.png`
- `rdm_method_comparison_panel__date_20260311__neural_order.png`
- `rdm_method_comparison_panel__date_20260313__neural_order.png`
- `rdm_method_comparison_panel__date_20260331__neural_order.png`

Generated tables include `per_date_method_comparison_summary.parquet` and three
date-specific neural RDM parquet files. The real-data summary has three dates
(`20260311`, `20260313`, `20260331`), each with 13 neural stimuli and 78 shared
upper-triangle entries per method. All per-date RSA similarities are finite.

Validation:

- `pixi run pytest tests/test_rsa_distance_method_comparison.py -q` -> `8 passed`
- `pixi run python scripts/run_rsa_distance_method_per_date_comparison.py` -> exit `0`

Committed the code change at `6ef0111`.

# Daily Log — 2026-04-06

## 16:01 — Refreshed Stage 1 reliability figures around focus-view interpretability

Reworked the Stage 1 reliability writer in `.worktrees/neutral-analysis-contract` so the weakest figures now emphasize the selected focus view instead of diluted cross-view summaries. The same-vs-different panel is now a focus-view violin plot, the old cross-view bar chart is now a per-stimulus same-versus-different dumbbell plot, and the old overlap-QC figure is now a date-by-stimulus availability heatmap.

- **Context**: The user wanted Stage 1 figures to communicate stability more clearly, specifically by making the same-versus-different separation more visible and by showing date-level stimulus coverage directly.
- **Decision**: Keep the reliability statistics and filenames stable, but replace the content of `same_vs_different_distributions.png`, `cross_view_reliability_comparison.png`, and `overlap_neuron_qc_summary.png` with stronger generic plots keyed off `focus_view`.
- **Rationale**: This preserves the existing output contract while materially improving interpretability across datasets, especially for runs where cross-date validation is limited by stimulus-panel mismatch.
- **Tags**: #figures #reliability #stage1 #visualization

## 19:55 — Added multi-version same-vs-different figure candidates

Extended the Stage 1 same-vs-different figure refresh in `.worktrees/neutral-analysis-contract` so the writer now emits three focus-view candidate plots instead of a single default image. The new outputs are `same_vs_different_distributions__raincloud.png`, `same_vs_different_distributions__violin_clean.png`, and `same_vs_different_distributions__boxen_points.png`.

- **Context**: After reviewing the first focus-view violin rewrite, the user wanted stronger aesthetics and asked for several candidate versions to choose from manually.
- **Decision**: Move same-vs-different summary text into a subtitle, keep a shared focus-view plot-prep layer, and stop requiring the unsuffixed `same_vs_different_distributions.png` output.
- **Rationale**: This keeps the statistical contract stable while making the figure contract more editorially flexible and visually stronger for publication-oriented selection.
- **Tags**: #figures #reliability #visualization #focus-view

## 21:01 — Refined raincloud candidate and added ECDF

Refined the focus-view `raincloud` candidate in `.worktrees/neutral-analysis-contract` into a lighter half-violin layout and added `same_vs_different_distributions__ecdf.png` as a fourth same-vs-different candidate. The new raincloud uses a white background, sparse offset points, a direct `Delta median` annotation, and shorter subtitle text.

- **Context**: The user judged the first raincloud candidate as readable but too busy, and explicitly asked to try the lighter half-violin treatment plus an ECDF comparison.
- **Decision**: Keep `violin_clean` and `boxen_points`, refine `raincloud` around median-gap readability, and add `ECDF` as an additional candidate without changing any reliability tables.
- **Rationale**: This makes group separation clearer under strong sample-size imbalance while preserving multiple publication-facing options for manual selection.
- **Tags**: #figures #reliability #ecdf #raincloud

## 21:17 — Simplified the boxen-points candidate and re-ran real data

Tuned `same_vs_different_distributions__boxen_points.png` in `.worktrees/neutral-analysis-contract` so it de-emphasizes the sampled points, uses softer boxen outlines, and moves the top-line interpretation to `Delta median` plus permutation `p`. Re-ran the `20260313_preprocess` reliability CLI into `results/202603_reliability_variant_check_v3`.

- **Context**: After comparing the candidate set, the user preferred `boxen_points` and `ECDF`, then asked for one more pass to make the `boxen_points` panel cleaner.
- **Decision**: Keep equal-count point sampling but reduce it to a lighter background layer, soften the boxen emphasis, and simplify the title area to the core comparison.
- **Rationale**: This keeps `boxen_points` readable as a result figure while making the distribution shift the first thing a reader sees.
- **Tags**: #figures #reliability #boxen #visualization

# Daily Log — 2026-04-04

## 10:25 — Reviewed stage-coupled naming and figure hierarchy debt

Reviewed the current post-Stage-3 codebase state and separated historical stage documentation from executable naming debt. Confirmed that stage labels still exist in exported helpers, CLIs, output writers, test fixtures, and generated run summaries, while the RSA writer still hard-codes a primary-versus-supplementary result hierarchy.

- **Context**: User wants the codebase to stop treating Stage 1/2/3 as part of the reusable architecture and no longer treat figures as main-versus-supplementary.
- **Decision**: Treat stage-prefixed APIs and stage-labeled run summaries as real portability issues, and treat the primary/supplementary figure contract in RSA outputs as a second, distinct naming problem.
- **Rationale**: Milestone language is acceptable in planning docs, but it reduces transferability when it appears in public code paths, file names, and generated artifacts.
- **Tags**: #review #naming #portability #figures

## 10:55 — Neutralized outward analysis contract in isolated worktree

Implemented the approved naming refactor in `.worktrees/neutral-analysis-contract`. Public helpers, CLI defaults, output directories, and run summaries now use neutral analysis names, while compatibility aliases remain for the old stage-prefixed APIs. RSA default figures and summary keys no longer expose a primary-versus-supplementary outward contract, but the underlying registry tier fields were intentionally left intact.

- **Context**: User approved moving old output directories to neutral names and approved removing primary/supplementary wording from outward figure and summary contracts without rewriting internal model taxonomy.
- **Decision**: Canonical outputs now write to `reliability/`, `geometry/`, and `rsa/`; canonical helpers now use responsibility-based names; legacy stage-prefixed APIs remain as aliases.
- **Rationale**: This removes the portability debt at the executable surface while avoiding a larger and riskier taxonomy rewrite in the validated ranking code.
- **Tags**: #implementation #naming #compatibility #rsa

## 11:09 — Neutralized remaining conftest fixture names

Renamed the residual `stage1_*` and `stage3_*` pytest fixtures in `tests/conftest.py` to neutral synthetic/preprocess/model-input names and updated their direct consumers. The fixture rename was verified both with targeted test runs and a full suite pass in `.worktrees/neutral-analysis-contract`.

- **Context**: After the outward naming refactor, the user asked to finish the remaining stage-prefixed fixture names in test scaffolding.
- **Decision**: Use neutral canonical fixture names without compatibility aliases because these fixtures are test-internal and all consuming tests live in the same repository.
- **Rationale**: Keeping legacy aliases in test scaffolding would preserve the exact naming debt the cleanup was intended to remove, with no external compatibility benefit.
- **Tags**: #tests #fixtures #naming #cleanup

# Daily Log | 2026-04-09

## 17:47 | Partially migrated durable memory to schema v1

Updated the live memory layer to start the gradual durable-schema migration. `MEMORY.md` now has a file-level schema header, the legacy `User Preferences` bullets were converted into two durable `preference` entries, and the legacy `Active Threads` bullets were converted into one durable `active-thread` entry. `memory/INDEX.md` was updated in the same write round so it points at the migrated sections and newly assigned durable IDs.

- **Context**: The migration plan called for the safest first slice to upgrade the live memory format without a big-bang rewrite. The highest-confidence candidates were `User Preferences` and `Active Threads`.
- **Decision**: Keep `Project Context`, `Architecture & Design Decisions`, `Lessons Learned`, and `Key References` in legacy form for now, while migrating only `Preferences` and `Active Threads` to schema v1 with stable IDs `MEM-20260409-001` through `MEM-20260409-003`.
- **Rationale**: This creates a usable hybrid memory file, preserves recall readability, and avoids fabricating metadata for lower-confidence legacy sections.
- **Tags**: #memory #migration #index #durable-schema

## 17:55 | Migrated Decisions and durable Lessons to schema v1

Continued the live memory migration by converting `Architecture & Design Decisions` into `Decisions` entries and converting the clearly reusable part of `Lessons Learned` into `Lessons` entries. The migration added durable IDs `MEM-20260409-004` through `MEM-20260409-009`, updated `memory/INDEX.md` in the same write round, and intentionally left one stale legacy lesson about missing commit history unmigrated.

- **Context**: The migration plan called for the second slice to convert the next-most-stable durable knowledge after `Preferences` and `Active Threads`, while still avoiding over-normalizing weak or stale content.
- **Decision**: Migrate the two preprocessing decisions and four durable lessons, but do not preserve the old `no commit history` lesson as a durable entry because it appears stale as of the current repository state.
- **Rationale**: This keeps the live durable memory trustworthy, adds retrieval metadata only for migrated entries, and avoids turning an outdated legacy note into a false durable fact.
- **Tags**: #memory #migration #decisions #lessons

## 17:59 | Migrated Project Context into durable Facts

Converted the legacy `Project Context` section into `Facts` entries and kept the migration limited to stable repository and dataset-layout facts. The migration added durable IDs `MEM-20260409-010` through `MEM-20260409-013` and updated `memory/INDEX.md` in the same write round.

- **Context**: The migration plan called for `Project Context` to be split carefully so stable cross-session knowledge moves into durable memory while current truth stays in `state/*.json`.
- **Decision**: Migrate only the stable repository-layout and matrix-input facts from `Project Context`, and continue treating active phase/status information as the responsibility of `state/project-status.json`.
- **Rationale**: This preserves the source-of-truth boundary between durable memory and machine-readable state while finishing the stable part of the live memory migration.
- **Tags**: #memory #migration #facts #repository-layout

# Daily Log — 2026-05-13

## 10:30 — IO 清理：移除 parquet/npz 写入函数

清理了 `io.py`，移除了所有 parquet/npz 相关的 I/O 函数（`read_parquet`, `write_parquet`, `write_tensor_npz`, `ensure_output_dirs`, `resolve_preprocessing_path`, `write_json`, `write_markdown_report`）。`io.py` 现在只导出 dataset 类、`AnalysisResult`、`save_analysis_result` 和 `read_metabolite_matrix`。

- **Context**: 用户指出当前 `io.py` 混合了两层职责，且大量 parquet/npy 文件被保存，输出应只包含 figure 和 summary Excel 文件
- **Decision**: `io.py` 只保留 dataset loading 和 result saving，移除所有中间文件写入能力
- **Tags**: #io #refactor #output-policy

## 11:00 — 提取 `_data_loaders.py` 和 `_vector_distance.py`

从 `model_space.py` 中提取了 `read_metabolite_matrix` 和 `build_stimulus_sample_map`（及其依赖的辅助函数）到新的 `_data_loaders.py`。从 `reliability.py` 中提取了 `compute_vector_distance` 到 `_vector_distance.py`（被 `features/neural.py` 依赖）。

- **Context**: `model_space.py` (863 行) 是旧 pipeline 的一部分将被归档，但其中 2 个函数被新代码使用；`reliability.py` 中的 `compute_vector_distance` 被 `features/neural.py` 依赖
- **Decision**: 提取必需函数到独立小模块，原模块整体归档
- **Tags**: #refactor #extraction #dependency

## 11:30 — 预处理 CLI 移除，preprocessing 仅作为 module

用户明确要求 "preprocess不应该作为cli存在，而是应以module的形式存在"。移除了 `cli/preprocessing.py` 和 `scripts/run_preprocessing.py`，归档到 `_legacy/`。`pixi.toml` 中的 `preprocess` 任务一并移除。

- **Context**: 预处理功能已由 `features/neural.py` 在构建 neural RDM 时自动调用，独立 CLI 不再必要
- **Decision**: preprocessing 只保留 module 形态，无 CLI
- **Tags**: #cli #preprocessing #module-only

## 14:00 — 旧 pipeline 归档至 `_legacy/`

将整个旧 reliability → geometry → RSA pipeline 及其相关文件移动到 `src/bacteria_analysis/_legacy/`，并在 `.gitignore` 中添加忽略规则。具体归档内容包括：

**源模块 (20个)**：`reliability.py`, `geometry.py`, `rsa.py`, `model_space.py`, `model_space_seed.py`, `neural_features.py`, `chemical_features.py`, `analysis_plotting.py`, `biological_subspace.py`, `anchor_effects.py`, `taxonomy_rsa.py`, 以及所有 `*_outputs.py`, `*_aggregated_responses.py`, `*_prototypes.py`
**CLI**：`cli/reliability.py`, `cli/geometry.py`
**测试 (12个)**：所有 pipeline 相关的测试文件
**脚本 (12个)**：`scripts/run_reliability.py`, `run_geometry.py`, `run_rsa.py`, `build_model_space.py` 以及所有 review 脚本

- **Context**: 用户要求 archive 旧 pipeline，且需要 gitignore
- **Decision**: 旧代码保留在磁盘上供参考，但不再被 git 跟踪和分析层使用
- **Tags**: #archive #legacy #pipeline

## 14:30 — 最终验证

97 tests passing。新包结构如下：

```
src/bacteria_analysis/
  __init__.py       io.py       rdm.py       stats.py       plotting.py
  preprocessing.py  constants.py
  _data_loaders.py  _vector_distance.py
  _analysis_dataset_impl.py  _analysis_results_impl.py
  analysis_dataset.py  analysis_results.py
  features/     anchor.py, chemical.py, neural.py, taxonomy.py
  analyses/     anchor_batch_effect.py, rdm_alignment.py, chemical_class_rsa.py
  cli/          __init__.py (empty, explanatory)
  _legacy/      (archived, gitignored)
```

- **Tags**: #verification #package-structure #tests

## 16:20 — RDM 方法边界整理

按照用户确认的结构，将当前维护的 distance-matrix/RDM/RSA-style 逻辑收敛到
`src/bacteria_analysis/analyses/rdm/`：

- `core.py`: RDM 对齐、上三角提取、相似度
- `builders.py`: `build_neural_rdm`, `build_chemical_rdm`, `build_chemical_class_rdms`
- `stats.py`: label shuffle、date-preserving shuffle、stimulus subset resample
- `plots.py`: RDM heatmap、null/stability plot、anchor RDM plot
- `neural_chemical.py`, `chemical_class.py`, `anchor_batch.py`: 三个当前维护的 RDM 分析入口

`features/` 保留为共享 feature/metadata 层：`neural.py` 只构建 neural feature
matrix 和 prototypes，`chemical.py` 只构建 chemical feature matrices，RDM
生成不再放在 `features/` 中。更新测试引用和 refactor 文档后，`pixi run test`
通过 97 项。

- **Context**: 用户指出 anchor/batch effect 与 neural-chemical comparison 都是 dissimilarity/RDM 方法，应避免未来 PCA 等非 RDM 方法混入
- **Decision**: 根包保持领域名 `bacteria_analysis`；方法边界使用 `analyses/rdm/`
- **Tags**: #analysis-refactor #package-structure #rdm

## 19:55 — Anchor-stimulus 参考图复刻

用户要求当前 refactor 后的 anchor-stimulus 图尽量复刻旧参考文件夹。将
`src/bacteria_analysis/analyses/rdm/plots.py` 中的 anchor 专用绘图函数改回旧
review 脚本的视觉合同：`magma/magma_r` 色板、`dpi=220`、固定 figsize、无
`bbox_inches="tight"`、MDS 使用 stimulus 颜色/date marker/同 stimulus 连线、
date-pair heatmap 保留单 stimulus 小面板和数值标注、full-trajectory heatmap
使用自定义发散色板和独立 colorbar 轴。真实数据临时验证中，用户点名的
`mds_projection`, `date_pair_heatmap`, `date_rdms__neural_clustered`,
`heatmap__full_trajectory`, `same_vs_other_stimuli` 对应 PNG 与参考图像尺寸和
像素差异均为 0。`pixi run test` 通过 97 项。

- **Context**: refactor 后当前绘图参数、颜色和布局与
  `results/202604_without_20260331/date_controlled_rsa_review/anchor_stimulus_date_effect/figures`
  中的参考图不一致
- **Decision**: 保持函数式 API，但为 anchor-review 图恢复参考绘图合同
- **Tags**: #anchor-stimulus #figures #rdm #reference-compatibility
